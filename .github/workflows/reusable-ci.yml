name: Reusable CI Workflow

on:
  workflow_call:
    inputs:
      working_directory:
        description: 'Working directory for CI tasks'
        required: false
        default: './Gui/sp-gui-angular'
        type: string

jobs:
  ci:
    runs-on: ubuntu-22.04
    name: Build, Test, and Package
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          check-latest: true

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Install dependencies
        working-directory: ${{ inputs.working_directory }}
        run: pnpm install --frozen-lockfile

      - name: Run linting
        working-directory: ${{ inputs.working_directory }}
        run: pnpm lint

      - name: Build application
        working-directory: ${{ inputs.working_directory }}
        run: pnpm build:Azure

      - name: Run tests
        working-directory: ${{ inputs.working_directory }}
        run: pnpm test:ci

      - name: Install Azure Functions dependencies
        working-directory: ${{ inputs.working_directory }}/api
        run: pnpm install --frozen-lockfile

      - name: Build Azure Functions
        working-directory: ${{ inputs.working_directory }}/api
        run: pnpm build

      - name: Prune Azure Functions dev dependencies
        working-directory: ${{ inputs.working_directory }}/api
        run: pnpm prune --prod

      - name: Prepare deploy artifacts
        env:
          WEB_ROOT: ${{ inputs.working_directory }}
        run: |
          rm -rf ./deploy
          mkdir -p ./deploy/web ./deploy/api
          cp -R "$WEB_ROOT"/dist/gui/browser/. ./deploy/web/
          cp "$WEB_ROOT"/staticwebapp.config.json ./deploy/web/staticwebapp.config.json
          cp "$WEB_ROOT"/api/host.json ./deploy/api/host.json
          cp "$WEB_ROOT"/api/package.json ./deploy/api/package.json
          cp "$WEB_ROOT"/api/pnpm-lock.yaml ./deploy/api/pnpm-lock.yaml
          cp -R "$WEB_ROOT"/api/dist ./deploy/api/dist
          cp -R "$WEB_ROOT"/api/node_modules ./deploy/api/node_modules

      - name: Upload web deploy artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-deploy
          path: ./deploy/web
          if-no-files-found: error

      - name: Upload api deploy artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-deploy
          path: ./deploy/api
          if-no-files-found: error
